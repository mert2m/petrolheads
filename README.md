# Petrolheads Landing Page

Landing page for Petrolheads - Turkey's premier automotive enthusiast community.

## Getting Started

First, install the dependencies:

```bash
npm install
```

Then, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Build for Production

```bash
npm run build
npm start
```

## Project Structure

- `app/` - Next.js app router pages and layouts
- `components/` - React components (Navbar, Hero, About, etc.)
- `public/` - Static assets (images, fonts)
- `prd.md` - Product Requirements Document

## Tech Stack

- Next.js 14+ (App Router)
- TypeScript
- Tailwind CSS
- Framer Motion

## Notes

- Update Twitter/X links in components to your actual Twitter handle
- Replace placeholder gallery images with actual automotive photos
- Customize colors and content as needed

